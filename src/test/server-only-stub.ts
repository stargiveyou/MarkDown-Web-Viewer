/**
 * `server-only` 패키지의 테스트용 대체 모듈.
 *
 * 실제 `server-only`는 클라이언트 번들에 서버 코드가 섞이면 빌드를 깨뜨리는 가드다.
 * 유닛 테스트는 Node에서 직접 실행되므로 이 가드가 오히려 import를 막는다.
 * vitest.config.ts의 alias가 테스트 실행 시에만 이 파일로 치환한다.
 *
 * 프로덕션 빌드에는 관여하지 않는다 — 가드는 그대로 작동한다.
 */
export {};
